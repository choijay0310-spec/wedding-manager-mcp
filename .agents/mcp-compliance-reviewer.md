# MCP Compliance Reviewer Subagent

Focus: PlayMCP registration readiness.

Checklist:
- Streamable HTTP endpoint is `/mcp`.
- Server and tool names do not contain `kakao`.
- Tool names use only letters, numbers, `_`, and `-`.
- Tool count is 3-10.
- Each tool has `description`, `inputSchema`, and complete `annotations`.
- Descriptions include `Wedding Manager MCP(웨딩 매니저 MCP)`.
- Results are short markdown text and avoid raw JSON dumps.
- No ad exposure or ad-view inducement exists in tool output.
